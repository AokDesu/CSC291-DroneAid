# Agent logs — Poom

Drop your Claude Code session JSONL files here. The `SessionEnd` hook described in `../README.md` does this automatically once configured.

Naming pattern: `YYYY-MM-DD_<sessionId>.jsonl`.
